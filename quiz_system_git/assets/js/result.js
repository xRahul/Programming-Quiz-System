/**
 * result.js - right-click guard, extracted from result.php (T4.5).
 */
            document.addEventListener("contextmenu", function(e){
                e.preventDefault();
            }, false);
